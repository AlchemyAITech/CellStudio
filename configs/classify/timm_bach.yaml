task: classification
model:
  backend: timm
  architecture: resnet18
  pretrained: true

data:
  data_dir: datasets/classfication/ICIAR2018_BACH_Challenge
  batch_size: 4
  num_workers: 4

training:
  epochs: 2
  learning_rate: 0.001
  save_dir: runs/classfication/bach

env:
  device: cpu
