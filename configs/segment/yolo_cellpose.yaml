task: segmentation
model:
  backend: yolo
  architecture: yolov8n-seg
  pretrained: true

data:
  data_dir: datasets/segmentation/cellpose
  batch_size: 4
  num_workers: 4

training:
  epochs: 1
  learning_rate: 0.001
  save_dir: runs/segmentation/cellpose

env:
  device: cpu
