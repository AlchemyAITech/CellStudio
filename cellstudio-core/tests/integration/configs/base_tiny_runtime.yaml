env:
  launcher: 'none'
  backend: 'nccl'
  device: 'cuda'
  seed: 42
  deterministic: False

runner:
  type: 'EpochBasedRunner'
  max_epochs: 1

use_amp: False

default_hooks:
  logger_hook:
    type: 'TextLoggerHook'
    interval: 1
  optimizer_hook:
    type: 'AmpOptimizerHook'
    grad_clip: 
      max_norm: 35
      norm_type: 2
  ema_hook:
    type: 'EMAHook'
    decay: 0.9999
  checkpoint_hook:
    type: 'CheckpointHook'
    interval: 1
    save_best: 'auto'
    rule: 'greater'

val_evaluator:
  metrics: []
  plotters: []
